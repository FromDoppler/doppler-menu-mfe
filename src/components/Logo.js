const Logo = () => {
  return (
    <div className="logo" aria-label="company logo">
      <a href="/dashboard">
        <span
          className="ms-icon icon-doppler-logo"
          role="img"
          aria-label="logo image"
        ></span>
      </a>
    </div>
  );
};

export default Logo;
